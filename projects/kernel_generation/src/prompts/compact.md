Summarize the conversation above into a compact brief for your future self, so you can keep optimizing after older messages are dropped from context. Be terse and concrete — this brief replaces the conversation, so anything you omit is lost.

Do NOT continue the conversation, answer any question in it, or call any tool — your ONLY output is the brief itself, in the exact structure below.

If a prior brief already appears earlier in the conversation, treat it as the base and UPDATE it in place rather than regenerating it cold: preserve every detail that is still true, drop only what has become stale, merge in what is new, and move items from "Active" to "Done" as their evaluations settle. When in doubt, keep it — a fact silently lost here is lost for the rest of the run.

Preserve exact file paths, symbols, function/kernel names, shell commands, error strings, and commit/node hashes VERBATIM — copy them character-for-character, never paraphrase them.

An authoritative GROUND-TRUTH block (current confirmed best, what is on disk vs. that best, and the confirmed lineage) will be prepended above your brief and overrides it. Do NOT restate the best score from memory, and never claim a result was "lost" or came from a different approach — defer entirely to that block for the standings, and spend your brief on the reasoning, decisions, and next moves it does not capture. Output only the brief in the exact structure below.
