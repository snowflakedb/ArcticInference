You have spent several turns without `evaluate`. Local timings are only advisory; the official score comes from `evaluate`, and every `evaluate full` is automatically recorded as a candidate the search keeps (there is no submit step).

Your priority now is to land a **first correct scored candidate**, not the fastest possible kernel. If you have a custom-kernel candidate, run `evaluate correctness`, then `evaluate full`. If you are still orienting, finish the *quick* orientation — the CUDA feature-set compiler target sections, the backend docs for the probed GPU, and a one-pass survey of the docs tree into `notes/DOC_INDEX.md` (skim each doc; abstracts only for papers, no figures) — then write the smallest *correct* hand-written CUDA kernel for the op (it does not need any exotic hardware feature or the state-of-the-art algorithm yet) and get it through `evaluate full`. That quick orientation, including the doc survey, is expected — it is not "churn"; what wastes turns is open-ended unscored exploration with no baseline. Get a correct slow kernel on the board, then optimize toward the fast path (guided by the paper you flagged). Do not assume previous-generation architecture-specific features carry forward; verify exact-target or compatible-family support in the mounted docs. The candidate must be a hand-written CUDA/PTX kernel, not a call/wrapper around cuBLAS, cuDNN, CUTLASS, Triton, torch library ops, or vendor kernels.
