You have gone a stretch without a new best. Re-orient — but first decide which case you are in, because they need opposite responses.

- **A structural build that just passed standalone validation but has not yet been scored** (e.g. a standalone hardware-feature test that hit `maxerr=0` this episode, integrated but not yet through `evaluate full`): getting it to **ONE scored `evaluate full`** is the single highest-leverage move you can make — do that NOW, before anything else. A just-validated integration is the OPPOSITE of a stall, and it is exactly the case the ground-truth anchor's "on disk ≠ confirmed best" line will tempt you to throw away. Do NOT revert it, fall back to the known-good incumbent, or declare the sub-approach dead until it has had at least one scored eval. The search already keeps your confirmed best safe, so scoring the new build costs you nothing and can only add a candidate.
- **Mid-way through a committed structural build** (a from-scratch rewrite onto the newest architecture-specific features you decided is the way past this plateau, not yet validated): keep going. Such a build normally spans many turns with no new best. Do NOT abandon it here — the search keeps your best *confirmed* candidate, so the attempt is safe and costs nothing if it fails. Drop only a *specific* sub-approach that an `evaluate` has shown to be a dead end, then pursue the same structural win by another route.
- **Grinding incremental micro-tweaks** (re-running near-identical variants, re-confirming an already-flat metric, or retrying something already measured worse): stop — that is the failure mode this message targets.

**Wall-clock triage.** Check `time_left` (or the remaining-time line in the note above). The scored `evaluate full` is what makes a build count, so if you are deep in a structural rewrite, drive it to ONE scored `evaluate full` well before the wall — the run ends at the wall and only scored candidates survive. This is about *pacing* the rewrite so it lands scored, NOT about retreating to safe incremental margins: the ceiling is the objective. Everything you write under the workspace (notes, sources, validated standalone test kernels, backups) persists across turns and context resets, and the search always keeps your confirmed best, so nothing you build is ever lost — you never need to defensively bank an incremental path.

Then:

1. If you have **no** validated artifact in flight and do not yet have a correct scored kernel, that is the priority — land the smallest correct custom kernel and `evaluate full` it (every full eval is auto-recorded as a candidate; there is no submit step).
2. Review the best candidate so far, recent failed attempts, the probed hardware block, and — usually the highest-leverage move — the docs and papers you flagged in `notes/DOC_INDEX.md`. If you have not surveyed the docs tree, or have not read the design of the paper relevant to your current bottleneck, do that now: the fast-path schedule you are missing is far more likely to be described in a paper than derivable from the ISA reference. `view` only the specific figure a section needs.
3. Name one concrete bottleneck and one structural lever grounded in that reading.
4. Make one focused change, then evaluate.

On NVIDIA, if the run has mostly used calls/wrappers around cuBLAS, cuDNN, CUTLASS, Triton, torch library ops, vendor kernels, DSL kernels, framework/vendor source trees, or older abstractions chosen from memory, pivot now to raw CUDA/PTX under `solution/` grounded in the docs for the detected `cuda_arch`. Look for features that are new to this GPU generation before accepting an older path.
